name: My Workflow
on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run My GitHub Action
      uses: ./ # This should be the relative path to your GitHub Action